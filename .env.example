# Only needed on machines where an antivirus/proxy does HTTPS scanning (e.g. Norton, BullGuard)
# and breaks certificate verification for yfinance/requests. See README for setup.
# CURL_CA_BUNDLE=.local/ca-bundle.pem
# REQUESTS_CA_BUNDLE=.local/ca-bundle.pem
# SSL_CERT_FILE=.local/ca-bundle.pem
